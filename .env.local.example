# Скопируй на сервер один раз:  cp .env.local.example .env.local && chmod 600 .env.local && nano .env.local
# Деплой и curl НИКОГДА не трогают .env.local — секреты живут здесь.

OPENAI_API_KEY=
# OpenRouter: OPENAI_BASE_URL=https://openrouter.ai/api/v1  и модель вида openai/gpt-4o-mini
OPENAI_BASE_URL=
OPENAI_MODEL=

POSTGRES_PASSWORD=
OWNER_TOKEN=
MEMBER_TOKEN=
