# Вся конфигурация в infra/compose.prod.yml (порты 8000 и 8080 зафиксированы там).
# Так команда «docker compose up» в корне не поднимет случайный старый вариант с ${APP_PORT}.
name: my_assistant

include:
  - path: infra/compose.prod.yml
